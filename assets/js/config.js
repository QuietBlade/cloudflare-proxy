/**
 * Cloudflare Proxy — Site Configuration
 * ========================================
 * Change DOMAIN below to your actual deployment domain.
 * Everything else auto-updates on page load.
 */
window.CF_PROXY = {
  DOMAIN: 'docker.yuanzhangzcc.com',
};
